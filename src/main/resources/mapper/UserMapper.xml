<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.syncforge.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.syncforge.user.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByUsernameAndPasswordHash" resultMap="UserResultMap">
        SELECT *
        FROM users
        WHERE username = #{username}
          AND password_hash = #{passwordHash}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.example.syncforge.user.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, password_hash, status)
        VALUES (#{username}, #{email}, #{passwordHash}, #{status})
    </insert>
</mapper>
