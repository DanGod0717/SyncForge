<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.syncforge.user.mapper.RefreshTokenMapper">

    <resultMap id="RefreshTokenResultMap" type="com.example.syncforge.user.entity.RefreshToken">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tokenJti" column="token_jti"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="revokedAt" column="revoked_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.syncforge.user.entity.RefreshToken"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (user_id, token_jti, token_hash, expires_at)
        VALUES (#{userId}, #{tokenJti}, #{tokenHash}, #{expiresAt})
    </insert>

    <select id="findByTokenJti" resultMap="RefreshTokenResultMap">
        SELECT *
        FROM refresh_tokens
        WHERE token_jti = #{tokenJti}
        LIMIT 1
    </select>

    <update id="revokeByTokenJti">
        UPDATE refresh_tokens
        SET revoked_at = #{revokedAt}
        WHERE token_jti = #{tokenJti}
          AND revoked_at IS NULL
    </update>
</mapper>

